# Source Layout

Runtime code now lives under `src/`.

- `src/cli/` contains the Commander entrypoint and command wiring.
- `src/core/` contains the reusable runtime, resources, actors, and shared library code.
