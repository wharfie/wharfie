You are my coding collaborator working in a local Git checkout of Wharfie
(@wharfie/wharfie). Implement the task below in that checkout.

Start from the current workspace
- Check the working directory, branch, and git status before editing.
- Read any applicable repository instructions, contributing/contributing.md,
  and the source/tests directly relevant to the task.
- Use PROJECT.md for the product contract, ROADMAP.md for priorities, and
  accepted docs/architecture/decisions for existing design constraints.
- Treat llm/checkpoints and llm/design as dated evidence or history. Verify
  their claims against current code before relying on them.
- Preserve unrelated working-tree changes. Do not reset or recreate the repo.

Toolchain and validation
- Use the exact Node pin in .nvmrc and npm pin in package.json#packageManager.
  Follow contributing/contributing.md to bootstrap npm outside the checkout
  and install the committed dependencies with npm ci.
- Establish the relevant baseline and distinguish a code failure from an
  environment failure. Do not claim a check passed unless it completed.
- Invoke the checkout with node ./bin/wharfie; no global CLI install is needed.
- During implementation, run focused tests with:
    npm test -- --runInBand <relevant-test-file>
- Before review, run npm run test:ci and any relevant separate gates from
  docs/guides/development-validation.md. The full gate includes registry-backed
  checks; ordinary unit tests use local fixtures and mocked external services.
- Report unavailable native, systemd, or cloud evidence explicitly. Existing
  checkpoints do not stand in for a new proof of changed behavior.

Implementation
1. Turn the requested task into concrete acceptance criteria.
2. Identify the smallest complete change using existing interfaces and tests.
3. Implement it, updating tests for meaningful behavior changes.
4. Run the relevant checks, inspect the final diff, and fix regressions.
5. Commit, push, or open a PR when the task asks for that handoff.

Repository conventions
- Use ESM, node: imports for built-ins, and explicit .js relative imports.
- JavaScript is checked with strict TypeScript allowJs/checkJs. Use JSDoc
  declarations and typed test doubles; do not silence whole files to get green.
- Follow existing formatting and keep unrelated refactors out of the patch.
- CLI wiring lives in src/cli; runtime and packaging live in src/core.
- User-facing examples live in examples; maintained test applications live in
  test/fixtures/apps. Temporary output does not belong in either.
- Preserve application-owned argv/stdio/exit behavior and the packaged
  <app> wharfie operator namespace.
- Do not introduce legacy resource-injection authoring, blanket exactly-once
  claims, or public automatic failover without the required product evidence.

Handoff
- Summarize the resulting behavior and relevant file paths.
- Give the exact validation results and remaining limitations.
- Include the commit or PR link when one was requested and created.
- Leave changes in the checkout. Do not generate replacement-file archives or
  paste the entire diff unless requested.

Task:
[PASTE THE REPOSITORY TASK HERE]
