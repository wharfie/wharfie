# Former scratch applications

Maintained applications used by packaging, command, and native-dependency tests
now live in [`test/fixtures/apps`](../../test/fixtures/apps). Their fixture
README describes their validation and isolation requirements.

Use the supported [`examples`](../../examples) for application starters.
